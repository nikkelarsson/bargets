# bargets.yaml - example config for barget's widgets
# Uncomment the options you wish to change ^_^

# cpu:
    # unit: fahrenheit  # default: celcius
    # prefix: "cpu: "   # default: ""
    # suffix: " ^_^"   # default: ""
    # indicator: "°"    # default: "°C"


# battery:
    # Suspend system when threshold.critical is met?
    # suspend: false  # default: true

    # Thresholds
    # threshold.full: 95  # default: 99
    # threshold.low: 10  # default: 5
    # threshold.critical: 5  # default: 3

    # Widget looks
    # indicator: "#"  # default: "%"
    # symbol.discharging: "«"  # default: "↓"
    # symbol.charging: "»"  # default: "↑"

    # Notification content
    # subject.low: "Battery alert"  # default: "Battery"
    # subject.full: "Battery notification"  # default: "Battery"
    # body.low: "Battery is very low!"  # default: "Battery charge low"
    # body.full: "Battery is now fully charged!"  # default: "Battery fully charged"
